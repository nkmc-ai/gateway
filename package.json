{
  "name": "nkmc-gateway",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
